- @expense.project = @project
- form_for :expense, @expense, :url => project_expense_path(@project.id, @expense.id) do |f|
  = hidden_field :_method, :put
  = render :partial => "form", :locals => { :form => f }
  %p= submit_tag "Update"
