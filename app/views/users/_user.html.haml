%tr
  %td= user.id
  %td= user.email
  %td= link_to "Edit", user_url(user.id), :class => "Edit"
  %td= link_to "Delete", user_url(user.id), :method => :delete, :confirm => "Are you sure you want to delete #{user.email}?", :class => "Delete"
