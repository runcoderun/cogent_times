- form_for :project_category, @project_category, :url => project_category_path(@project_category.id) do |f|
  = hidden_field :_method, :put
  = render :partial => "project_categories/form", :locals => { :form => f }
  %p= submit_tag "Update"
