- form_for :system_setting, @system_setting, :url => system_setting_path(system_setting.id) do |f|
  = hidden_field :_method, :put
  = render :partial => "system_setting/form", :locals => { :form => f }
  %p= submit_tag "Update"
