%tr
  %td= status.id
  %td= status.description
  %td= link_to "Edit", status_url(status.id), :class => "Edit"
  %td= link_to "Delete", status_url(status.id), :method => :delete, :confirm => "Are you sure you want to delete #{status.description}?", :class => "Delete"
